apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-reverse-injector.fullname" . }}
  labels:
    {{- include "token-reverse-injector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "token-reverse-injector.selectorLabels" . | nindent 4 }}
